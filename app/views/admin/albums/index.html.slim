#albums
  /! top toolbar
  .btn-toolbar
    .btn-group
      = link_to new_admin_album_path, class: 'btn btn-primary' do
        i.icon-file-text
        span.l-padding-5 = t("breadcrumbs.admin.albums.new")
  .widget-box.transparent
    .widget-body
      .widget-main
        .table-header = t('breadcrumbs.admin.albums.index')
        table#albums-datatables.table.table-striped.table-bordered.table-hover data-source="#{admin_albums_path}"
          thead
            tr
              th = Resource.human_attribute_name :title
              th = t('breadcrumbs.admin.albums.assign')
              th = Resource.human_attribute_name :created_at
              th = t('buttons.actions')
            tr
              th = Resource.human_attribute_name :title
              th
              th
              th
