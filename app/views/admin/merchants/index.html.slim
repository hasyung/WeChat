#merchants
  /! top toolbar
  .btn-toolbar
    .btn-group
      = link_to new_admin_merchant_path, class: 'btn btn-primary' do
        i.icon-file-text
        span.l-padding-5 = t("breadcrumbs.admin.merchants.new")
  .widget-box.transparent
    .widget-body
      .widget-main
        .table-header = t('breadcrumbs.admin.merchants.index')
        table#merchants-datatables.table.table-striped.table-bordered.table-hover data-source="#{admin_merchants_path}"
          thead
            tr
              th = Resource.human_attribute_name :title
              th = t("breadcrumbs.admin.merchants.assign")
              th = Resource.human_attribute_name :created_at
              th = t('buttons.actions')
            tr
              th = Resource.human_attribute_name :title
              th
              th
              th
